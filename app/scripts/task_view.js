export default function taskView(store) {

    let $html = $(`
        <div id="task-form">
            <input id="name" type="text" placeholder="task">
            <input id="description" type="text" placeholder="description">
            <input id="">
            <button id="login-submit-btn">submit</submit>
        </div>
    `);


    //renderTodo = function(item, index, array) {
    //    console.log("item ", item);

    //    $('#todo-list').append(element);
    //    let $html = $(`
    //        <li>${item.name}</li>
    //    `);

    
    return html
}
